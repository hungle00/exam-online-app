<%= form_with(model: exam) do |form| %>
  <% if exam.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(exam.errors.count, "error") %> prohibited this exam from being saved:</h2>

      <ul>
        <% exam.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <h5><%= form.label :title %></h5>
    <%= form.text_field :title, class: "form-control", placeholder: "Exam title" %>
  </div>

  <div class="form-group">
    <h5><%= form.label :category %></h5>
    <%= form.collection_select :category_id, Category.all, :id, :name, class: "form-control" %><br>
  </div>

  <div class="form-group">
	  <h5><%= form.label :time %></h5>
  	<%= form.number_field :time, class: "form-control", placeholder: "Time in minute" %>
  </div>

  <div class="actions">
    <%= form.submit class: "btn btn-primary mt-2"%>
  </div>
<% end %>
